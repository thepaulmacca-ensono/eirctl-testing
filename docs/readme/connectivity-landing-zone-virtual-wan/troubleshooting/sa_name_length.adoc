=== Hub Provisioning Timeout

Virtual Hub provisioning can take 15-30 minutes. If Terraform times out, increase the timeout value or check the Azure Portal for provisioning status. Retry after the hub reaches a `Succeeded` state.
