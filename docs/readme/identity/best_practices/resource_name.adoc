=== Resource Naming

Follow Azure Cloud Adoption Framework naming conventions. Use `vnet-` for virtual networks, `snet-` for subnets, `nsg-` for network security groups, and `rg-` for resource groups.
